Đặt 2 file logo vào thư mục này:

  logo-phuong.png   → Logo Phường Cầu Ông Lãnh  (hiển thị bên TRÁI navbar)
  logo-congdoan.png → Logo Công đoàn Việt Nam     (hiển thị bên PHẢI navbar)

Định dạng hỗ trợ: PNG, JPG, SVG
Kích thước đề nghị: cao 120px trở lên, nền trong suốt (PNG/SVG tốt nhất)

Sau khi đặt file xong:
  git add .
  git commit -m "them logo phuong va cong doan"
  git push
